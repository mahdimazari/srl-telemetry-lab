"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.extractPluginExtensions = exports.extractPluginMeta = void 0;
const ts = __importStar(require("typescript"));
const utils_1 = require("./utils");
function extractPluginMeta(entry) {
    const extensionPoints = extractPluginExtensions(entry);
    return { extensions: extensionPoints };
}
exports.extractPluginMeta = extractPluginMeta;
function extractPluginExtensions(entry) {
    const program = ts.createProgram([entry], {
        allowSyntheticDefaultImports: true,
        allowJs: true,
    });
    const sourceFile = program.getSourceFile(entry);
    const checker = program.getTypeChecker();
    if (!sourceFile) {
        (0, utils_1.debug)(`Source file not found for entry "${entry}".`);
        return [];
    }
    const [appNode] = findAppPluginDeclarationNode(sourceFile, checker);
    if (!appNode) {
        (0, utils_1.debug)(`No app node found (This probably means that there was no AppPlugin class instantiated in the scanned code).`);
        return [];
    }
    const linkExtensionConfigs = (0, utils_1.getLinkExtensionsConfigs)(appNode, checker);
    const componentExtensionConfigs = (0, utils_1.getComponentExtensionConfigs)(appNode, checker);
    return [...linkExtensionConfigs, ...componentExtensionConfigs];
}
exports.extractPluginExtensions = extractPluginExtensions;
function findAppPluginDeclarationNode(entry, checker) {
    let appNode;
    const rootNodes = [];
    ts.forEachChild(entry, (node) => {
        rootNodes.push(node);
        // Check if it is a default export
        if (ts.isExportAssignment(node)) {
            // Supports when you do export default new AppPlugin()...
            if (ts.isCallExpression(node.expression) || ts.isNewExpression(node.expression)) {
                const type = checker.getTypeAtLocation(node.expression);
                if (type.symbol?.escapedName === 'AppPlugin') {
                    appNode = node;
                    return true;
                }
            }
            // Continue to the next node
            return false;
        }
        // Check if it is a named variable export
        if (ts.isVariableStatement(node)) {
            const isExported = node.modifiers?.find((mod) => {
                return mod.kind === ts.SyntaxKind.ExportKeyword;
            });
            if (!isExported) {
                // Continue to the next node
                return false;
            }
            const isPluginExport = node.declarationList.declarations.find((decl) => {
                if (!ts.isVariableDeclaration(decl)) {
                    return false;
                }
                if (!ts.isIdentifier(decl.name)) {
                    return false;
                }
                return decl.name.escapedText === 'plugin';
            });
            if (isPluginExport) {
                appNode = node;
                return true;
            }
            // Continue to the next node
            return false;
        }
        // Check if it is a named re-export
        if (ts.isExportDeclaration(node)) {
            if (!node.exportClause || !ts.isNamedExports(node.exportClause)) {
                // Continue to the next node
                return false;
            }
            const isPluginExport = node.exportClause.elements.find((ele) => {
                if (!ts.isExportSpecifier(ele)) {
                    return false;
                }
                if (!ts.isIdentifier(ele.name)) {
                    return false;
                }
                return ele.name.escapedText === 'plugin';
            });
            if (isPluginExport) {
                appNode = node;
                return true;
            }
            // Continue to the next node
            return false;
        }
        // Continue to the next node
        return false;
    });
    return [appNode, rootNodes];
}
